# 自动选样修复验证记录

验证日期：2026-09-10。本记录针对自动选样，不表示 Agent 试运行、正式验收或发布通过。

## 本轮实现

- 勾选自动查找或重新查找后立即显示参数选择对话框；选择待查字段并点击“开始查找”后，原位切换为进度对话框。启动、排队、阶段、实际模型/强度、耗时和终态分别展示。
- 关闭、Escape 和后台继续不取消任务；显式取消等待服务端清理确认。刷新恢复原区域进度，不自动弹窗或重复提交。
- 成功后跳转参数确认区，不自动确认或试运行。网络响应不确定时复用请求身份并先查询状态。
- 独立 600 秒预算：540 秒外部读取、60 秒证据校验和收尾；取消/到期关闭证据写入，未完成清理保持操作锁。
- 排序项只允许字段名。证据持久化后，对唯一源字段映射的必填标量等值输入优先按稳定业务键从同一行确定性选样，仍通过原有证据、范围和 Schema 校验。
- 不修改自由查询 1800 秒或编写对话 3600 秒时限，不修改草稿业务定义。

## 真实 SDK 与 SAP 只读检查

在独立 API、数据库和 data_root 中复制 `mm-po-gr-status` 当前修订进行检查，未写入原草稿或启动正式验收。

| 项目 | 结果 |
| --- | --- |
| SDK | openai-codex 0.147.0 |
| 模型 / 推理强度 | gpt-5.6-sol / max |
| 隔离运行 | agent_op_462a23e976364274a33e |
| 最终状态 | ready，参数仍需用户确认 |
| 时限 / 实际耗时 | 600 秒 / 108.7 秒 |
| 候选 / 数据读取次数 | 100 条 / 1 次 |
| 已核对参数 | 1 个，确定性选样 |
| 清理 | 测试进程正常退出，无遗留测试 API |

100 条是有界候选数量，不代表 SAP 全量数据完整。真实参数与原始行不纳入本记录；脱敏结果在忽略目录 `.local-data/sample-live-20260910-02/sample-check-result.json`。

第一次检查已生成样本，但发现 SDK 流取消后后台队列读取线程未退出。修复为先结束本作业拥有的 SDK 消息队列，再取消流消费。该兼容处理使用 0.147.0 的内部 router，增加了真实 MessageRouter 队列回归；升级 SDK 时应重新验证。第二次检查正常结束。

## 自动化及页面检查

- 相关 Python 回归：144 passed，1 skipped。
- Agent 草稿前端测试：29 passed。
- Astro：40 files，0 errors / warnings / hints。
- Agent 目录：32 个 Manifest 校验通过，30 个可见 Agent；生产构建 76 页成功。
- 中英文 × 1440 / 772 / 390px 的合成 API 浏览器检查全部通过：即时弹窗、启动期不可取消、键盘焦点约束、Escape 后台继续、刷新恢复、取消、重试、成功确认区焦点及不自动确认。浏览器检查不访问 SAP。
- 完整前端测试：71 passed，2 个既有失败。SD 模块数量断言仍期望 10 而目录为 9，另一个断言要求已停用 `billing-output-monitor` 的页面；本轮未修改该生命周期状态或放宽断言。
- `git diff --check` 通过。

复验入口：`scripts/check_sample_discovery_live.py`、`site/tests/sample-progress.browser.mjs`（显式执行，支持 `SAPBA_PLAYWRIGHT_MODULE` 和 `SAPBA_BROWSER_DIST`）。真实检查应使用新的隔离 data_root；浏览器检查使用合成 API，不替代真机证据。

## 服务与交付边界

首次验证时确认没有执行中的选样、编写或调度任务后，使用标准启动脚本受控重启 API 和 Preview，重新构建站点并通过健康检查。现有等待用户输入的查询继续保持 `waiting_input`，当时 `mm-po-gr-status` 为修订 3。该版本号仅记录当时状态，不限制用户后续编辑。

未提交、推送、发布或启用 Agent；历史记录未批量改写。新选样任务使用本轮 600 秒实现，历史缺失的计时字段显示未记录，不伪造历史时限。

## 同日补充：日期格式与字段选择

- 新请求支持 `selectedFields`。省略时兼容原有缺失必填字段选样；显式选择时，仅允许回填选中的未填写公开字段，包括可选字段。已填值保持查询范围，敏感值和复杂对象保持人工输入。
- 选择集合纳入幂等指纹及运行记录。未知、重复、空集合、已填写或敏感字段不能通过后端校验。
- 日期根据输入契约或本次权威 SAP 元数据识别，先校验原始单元格，再转换为 `YYYY-MM-DD`。原始行、行 Hash 和证据引用不变，另记转换类型。不以字段名字猜测日期，不任意截断未声明日期类型的时间戳。
- 本次反馈中的 `/Date(1507420800000)/` 转换为 `2017-10-08`，已覆盖有/无输入 Schema 日期格式的回归。
- 只选部分字段时，可以保留已核对样本并手工补齐未选的必填项；试运行仍需参数确认和完整输入校验。
- 新隔离真机检查 `agent_op_e14d8015b48d4d05a5c0`：144.7 秒、一次只读查询、100 条候选、一个显式选择的参数、确定性选样，日期 ISO 格式检查通过，进程正常退出。脱敏结果位于 `.local-data/sample-date-selection-live-20260910/sample-check-result.json`。
- 补充后相关 Python 回归 153 passed / 1 skipped；草稿前端 30 passed；Astro 无错误、警告或提示；76 页生产构建成功。
