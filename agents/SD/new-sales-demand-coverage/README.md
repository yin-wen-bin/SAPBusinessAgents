# 新增销售需求覆盖度检查助手

该固定 Agent 在外部销售需求录入 SAP 前，按物料模拟需求日期及后续期间的 MRP 可用量。它严格只读，不创建销售订单、不运行 MRP，也不替代正式 ATP 确认。

当前 `0.1.0` 支持同一工厂和 MRP 范围下 1–50 个物料，每个物料分别输入需求数量、日期和可选单位。结果按物料隔离，部分物料证据不足时保留其他结果并将整批标记为 `inconclusive`。本版本已通过直接 SAP、Codex 自由查询、逐物料固定查询和批量固定查询验收，可以执行。
