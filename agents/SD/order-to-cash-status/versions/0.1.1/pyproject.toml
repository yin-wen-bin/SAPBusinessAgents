[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sap-order-to-cash-status"
version = "0.1.0"
description = "Order-to-Cash Status"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
order-to-cash-status = "order_to_cash_status.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
order_to_cash_status = ["fixtures/*.json"]

[tool.pytest.ini_options]
pythonpath = ["src", ".."]
testpaths = ["tests"]
addopts = "-q"
