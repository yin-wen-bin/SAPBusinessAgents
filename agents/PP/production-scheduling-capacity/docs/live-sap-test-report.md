# 真实 SAP 测试报告：生产排程与产能

- 结论：**PARTIAL PASS**。计划订单与工作中心主数据可读；所选计划订单没有产能明细，未完成负荷率计算。
- 历史只读基线：计划订单 `312/313/542/543/544` 返回物料、工厂、数量和日期（case `659eec18-a3f5-40f4-ae08-1b39ed958548`）。
- 工作中心：工厂 `1710` 返回 `ASSEMBLY`、`TESTING`、`PACK01`、`PACK02`、`WINDING` 等 5 条记录（case `89e3c961-8007-44f7-84d8-e42a84db1360`）。
- 产能需求：计划订单 `312` 的 `A_PlannedOrderCapacity` 精确查询成功但为 0 行（case `194a4613-7d1f-42fe-bf15-accad1974871`）；因此不能把它当作有负荷的排程样本。
- SAPSkillhub：SE16N 成功导出 `PLAF` 10 行和 `CRHD` 10 行；PLAF 的 `542/543`、`SG21/1010` 与当时的只读 API 结果一致。
- 限制：未读取完整 KAKO/KBED 产能桶，也未建立计划订单到工作中心的完整链路，不能验收优化算法。
- 安全：未执行 CM21/CM25 派工或订单日期修改。
