# 真实 SAP 测试报告：生产订单执行监控

- 结论：**PASS（只读链路）**。订单、项目、工序、组件与物料凭证查询均得到明确结果。
- 订单发现：工厂 `1710` 返回生产订单 `1000000` 至 `1000004`（case `0eae9b93-0093-4e16-b19f-329fb95e39ec`）。
- 精确订单：`1000000` 项目计划量 `49 PC`、收货量 `0`、未完全交货（case `d15b99ba-1c4b-4369-961f-c42cacaf90cb`）。
- 工序：订单 `1000000` 有 `0010 Assembly` 和 `0020 Final Acceptance`，计划量均为 `49`、确认产量为 `0`（case `55571a51-df31-401c-b1e2-b6dc0418dd25`）。
- 组件：返回 9 个组件，需求量为 49 或 98，领料量和确认可用量均为 0，分页完整（case `4602b846-3ab2-4318-a97c-60a7a8910804`）。
- 物料凭证：移除错误的项目级 PostingDate 后，按订单查询成功且为 0 行（case `ce9495ff-498b-437d-877d-c1918820a73b`）；这是有效空结果。
- SAPSkillhub：AFKO、AFPO、AFRU 均成功导出 10 行；RESB 后续导出因会话/脚本状态失败，未用于最终结论。
- 安全：未执行确认、发料、收货、TECO 或订单变更。
