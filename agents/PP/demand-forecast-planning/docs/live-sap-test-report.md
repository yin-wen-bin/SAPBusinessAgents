# Historical live SAP report: demand-forecast-planning 0.2.0

Version `0.2.0` previously passed a single-material live test using `YS_FG_001 / 3000`. That test covered PIR, sales demand, planned orders and an external-demand simulation of `100000 PC`.

This report is retained only as historical evidence. The external-demand simulation has moved to SD `new-sales-demand-coverage`, and PP version `0.3.0` now uses a 1–50 material batch contract. The historical sample and run IDs do not validate the new contract and are not embedded in current defaults or rules.
