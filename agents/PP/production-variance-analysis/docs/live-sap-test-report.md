# 真实 SAP 测试报告：生产数量与物料差异根因分析 0.2.0

- 结论：**PASS**。直接 SAP 基线、自由查询和固定 Agent 对订单 `1001233` 的数量、组件、移动和完整性判断一致。
- 订单状态：已 TECO；计划数量 `7 PC`。
- 工序确认：经工序顺序校验后的最终工序确认产量为 `7 PC`，没有累加工序产量。
- 成品入库：订单项目权威入库数量为 `6 PC`；物料凭证中的一条 `101` 入库用于复核。
- 数量差异：入库比计划及最终确认少 `1 PC`。
- 组件：3 个组件均足额领料；物料凭证包含 5 条 `261` 领料。
- 冲销：未发现 `102/262` 或冲销引用。
- 业务结论：`attention`，候选原因是 `receipt_shortfall_after_confirmation`，业务文案为“工序确认7件，但库存只收到6件”。
- 成本：固定为“本 Agent 未评估”，不影响本 Agent 的证据完整性；成本差异由独立的 `product-cost-variance` Agent 负责。
- 安全：全部 SAP 操作为 GET；未执行确认、收发货、TECO、冲销、重估、结算或状态变更。
