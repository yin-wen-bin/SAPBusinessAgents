# 真实 SAP 测试报告：生产偏差根因分析

- 结论：**PARTIAL PASS**。数量维度可执行且显示尚无实际活动；工序废品字段存在当时的只读 API 索引与真机不一致；成本维度未闭环。
- 订单项目：订单 `1000000` 的计划量 `49 PC`、收货量 `0`、实际偏差 `0`、计划废品 `0`（case `d15b99ba-1c4b-4369-961f-c42cacaf90cb`）。这表示当前没有实际过账证据，不表示生产表现良好或差。
- 工序：两道工序计划量均为 49、确认产量为 0（case `55571a51-df31-401c-b1e2-b6dc0418dd25`）。
- 用料：9 个组件领料量均为 0，订单关联物料凭证为 0 行（cases `4602b846-3ab2-4318-a97c-60a7a8910804`, `ce9495ff-498b-437d-877d-c1918820a73b`）。
- schema 缺口：当时的只读 API schema 声明 `OpTotalConfirmedScrapQty`，实际 SAP 请求返回 404“找不到细分”（case `4e79ff9f-5d8e-41a9-90ba-acd313d70552`）；移除该字段后同一工序查询成功。
- SAPSkillhub：AFPO 与 AFRU 各导出 10 行；连续批导出在 AFRU 工作簿创建后未退出，脚本被单独停止，SAP GUI 保持打开；随后 RESB 导出返回 COM 数据未就绪。
- 限制：未取得同一订单的成本要素行，不能验收成本差异分解；未对 0 活动订单做根因推断。
- 安全：未执行冲销、重估、结算或状态变更。
