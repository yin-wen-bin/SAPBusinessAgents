# Intelligent Sourcing and RFQ Evaluation

Deterministic, GET-only MM Agent. Run `rfq-evaluate --format markdown`. It uses fixed 60/25/15 scoring and suppresses ranking when currency, unit, price unit, or required evidence is not comparable.
