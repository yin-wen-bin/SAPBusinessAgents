[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sap-intelligent-sourcing-rfq"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
rfq-evaluate = "intelligent_sourcing_rfq.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
