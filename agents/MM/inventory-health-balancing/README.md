# Inventory Health and Balancing

Deterministic, GET-only MM Agent. Run `inventory-health --format markdown`. Embedded SAP OData is primary; live-DDIC-validated `sap-adt-table-export` reads are conditional. MB5B is reserved for optional human balance reconciliation.
