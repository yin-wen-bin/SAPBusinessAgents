[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sap-cost-center-expense-anomaly"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
cost-center-anomaly = "cost_center_expense_anomaly.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
